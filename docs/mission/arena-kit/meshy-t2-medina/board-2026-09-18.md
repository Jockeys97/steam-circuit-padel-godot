# Board — Medina Mesh T2, 2026-09-18

## Shipped

- Charter, recon, inventory for the Medina API pack.
- Official request shape pinned: Mesh T2 + Smart Topology + 2K PBR texture on create.
- Ten intake PNGs already on disk. Ground texture stays out of Meshy.

## Verified

- `ai_model` = `meshy-t2` with `model_type` = `smart-topology` (docs.meshy.ai image-to-3d, fetched today).
- Texture is the same POST, not a later remesh.
- Existing GUI torii/medina models cannot be textured through the API (no task ids; Workspace is not enumerable).
- No Meshy key on this machine (profile `.env`, `~/.config/meshy`, process env).

## Blocked

Need a Meshy API key before any POST. List-price exposure for the ten Medina jobs is 150 credits (15 each, 2K textured T2). Measured cost is `consumed_credits` after the run.

## Next

Luca runs `/tmp/meshy-api-key-intake.sh` (hidden paste, live balance probe, writes `MESHY_API_KEY`). Then the pipeline captain generates the ten GLBs.
