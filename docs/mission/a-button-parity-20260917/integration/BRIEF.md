# Port integration: A-button differential proof

Read ../CHARTER.md, Reference/Port/Review reports and current source before changing test artifacts. Repository /Users/alessiofantini/Documents/steam-circuit-padel-godot. Mission docs/mission/a-button-parity-20260917. You are the named Port integrator continuing its first lane. Production and both JS trees read-only. Write only mission/integration/** and /tmp/padel-a-button-20260917-port/**. You may adapt COPY versions of the existing oracle/replay under integration; do not overwrite other lanes. Reuse their exact corpus and original untracked harnesses where valid. No fixes, installs, paid APIs, nested agents, commits, deletions, focus changes or killing user game. One diagnostic Godot at a time, isolated project/cache only.

CEO independently reran reference/verify.mjs and port/run.sh: exit 0, reproducible browser trace, port PASS 32/32 and SCRIPT ERROR=0; live user game survived. Production hashes match baseline. THESE ARE SEPARATE CHECKS, NOT CROSS-ENGINE PARITY.

## Important defects in current proof contract
1. Reference corpus uses dt=1/60, production fixed step is 1/120. Keep original corpus provenance, but create explicitly derived production-rate corpus or scheduling adapter and document change. Never call 1/60 a production fixed tick.
2. Reference oracle currently polls then updateMatch once per frame. Real browser has separate gamepad/game rAF loops and calls getInput before accumulator; must test zero/one/multiple substeps with actual production frame glue or extracted minimal actual block. No manually rewritten scheduler claimed as production. If extraction not achievable, report exact limitation.
3. Review points to deliberate edge latch divergence: Godot retains a release on a frame with zero substeps while browser clears it. Reproduce with a genuine A press/hold/release timeline; do not feed hit=true directly as a substitute for the button path. Compare following tick queued shot power/intent and, if feasible, final contact. Distinguish loss before contact from late contact. Test 240/144 versus 60 Hz schedules with a pinned rAF ordering; alternative ordering is a bound, not physical latency evidence.
4. B1 trigger range was hypothetical, NOT known faulty hardware. Godot may already normalize triggers. Do not label as bug or most probable cause absent actual API/hardware evidence. No live pad manipulation in this mission.
5. Report wording 'physical pad' is wrong for dictionaries; use 'injected device samples into production sampler'. Unchanged presentation diff does not rule out a visual-feel difference; it only rules out input logic changes in that diff.
6. port/run.sh ignores import errors and uses --quit-after as timeout, which can false-green. Your integration runner must fail if timeout, missing completion marker, count mismatch, SCRIPT ERROR, or unapproved ERROR. Use Python subprocess timeout for real bounded processes and preserve stdout/exit code. Explicit narrow named teardown warning may be reported but not hide others. No set of printed PASS strings alone is enough.

## Required execution
A. Feed identical corpus device samples and initial sim states into actual JS and Godot production sampler + simulation. Compare all named input/queue/shot fields; explicit representation-only normalization list, no unexplained exclusions or blanket tolerance. Print first divergence per scenario and total comparisons.
B. Test the real fixed-step queue retention sequence above, including original browser clear behavior. A detected intended divergence is a successful diagnosis, NOT a parity pass. Separate verdicts for common fixed-rate behavior and frame-glue divergence.
C. Prove deterministic reruns and a red control using the ACTUAL cross-engine comparator on a corrupted copy of its input, not generic diff as a proxy. Fail correctly on empty/truncated outputs too.
D. Write REPORT.md early and incrementally, final summary.json and run logs with exact commands, hashes, counts and first divergence; include remaining gaps and a single bounded command CEO/reviewer can rerun.
E. Avoid spending on broad suites. Target <=35 tool calls. If integration cannot reach final contact in all cases, record input/queue level evidence and leave feel gate open rather than expand indefinitely.

Final short Italian return: handles, command, first differences or scoped match result, limitations and budget. This is launch 4/6, 2 shared remain after this dispatch, retries max2 per gate within launch ceiling. Paid API allowed/used=0, subscription tokens cost unknown.
