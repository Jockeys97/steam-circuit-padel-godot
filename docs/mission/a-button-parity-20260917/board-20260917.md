# Board: tasto A, esito finale

## Verdict
Narrow diagnosis verified by Astra and independently reviewed: a scripted A release on a frame without a simulation substep is lost by the browser, retained by Godot and queued on the following step. Godot uses the fallback variant auto in this case. No claim about final shot degradation or Luca's exact perceived symptom.

## Evidence
Parent reran integration/test_comparator.py: 8 passed, 0 failed, exit 0.
Parent reran integration/run_integration.py: exit 0, 67/67 frames, 2278 compared field pairs, 14 documented queue differences, no additional differences in these traces. Both engines deterministic across repeats; corruption adds a detected difference; no-op corruption rejected as a negative control; truncation detected. Actual traces contain every named input and queue field, separately checked by parent.
Source hashes for tracked input_map, match_controller, sim and both JS main/game references match baseline. No gameplay changes by this mission.

## Remaining limitations
Full harness certification remains withheld: nested required fields missing from BOTH traces are not rejected by comparator._validate. Parent injected that omission into copies in memory and obtained a false PASS. The current real traces have no missing fields, so this does not invalidate the observed diagnosis. latch_reproduced checks the set of field names, not exact frame/value pairs; do not describe it as a fully pinned numeric regression oracle.
Synthetic frame schedules only, injected device samples, queue-level evidence. No matched final-contact, physical controller, aim-loss or human feel proof. Review accepted the narrow diagnosis before the final comparator repair; no separate worker reviewed that final delta. Parent inspected and reran it and records remaining gaps here.

## Budget and stopping point
6/6 native worker launches consumed. No paid API calls. Subscription cost unknown. No further launch or production fix authorized under this charter.

## Recommendation
Preserve the entire shot request when deferring it, rather than only the hit flag, if a follow-up test demonstrates loss of requested type or aim. Do not copy the browser's dropped-input behavior just to force equality. Before a product fix, reproduce contact with matching initial state and a normal charge/release timeline.

## Repository concurrency
HEAD changed from baseline 918bfa9e609200284376fc0d2abe604a5d7510c7 to ed23b82bcc91ca63d4f3dc6faebb75d9de0ea667 during this mission. This session did not commit or push. Do not attribute that concurrent history change to this mission. Relevant production hashes still match baseline.

Evidence: integration/REPORT.md, integration/summary.json, review/FINAL.md, baseline.json.
