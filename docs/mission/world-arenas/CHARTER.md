# Five native world arenas

## Outcome and limits

Recreate torii, medina, carioca, aurora and egeo using native Godot geometry and generated textures under godot/game/arenas/. References under art/concepts/world-arenas-r1/ are read-only and must never be imported or regenerated. Preserve the existing modified reference README. No commits, pushes, paid API calls, provider changes or deletions. Additional spend cap $0 and zero generation credits. Existing OpenCode Go subscription only; telemetry unavailable means unknown, not zero inference cost.

## Authority and proof

The whole court, both baselines, both side walls and rear rebound glass must remain visible. Scenery stays behind BACKDROP_Z=-8.0. Real camera presets are framing authority. No scenery may occlude play. Reject violations rather than hiding them in captures. Owner taste remains unapproved but does not stop technical execution.

Gates: five distinct selectable arenas; real preset captures with SHA-256 manifests; field-law checks; green game_slice_test.gd with exact tallies and runtime errors separately counted; independent reviews and current evidence ledger. Freeze each capture and an eight-item checklist before council scoring. Two rounds maximum, one refinement; record missing cross-family capacity honestly rather than describing same-family reviews as heterogeneous. No paid fallback.

## Ownership

Astra owns routing, verification and reporting only. Arena integrator owns godot/game/arenas/ and necessary arena_scenery integration, and later combined proof. Proof engineer owns new world-arena tests and capture tooling only, not production code. Independent critics are dispatched after captures freeze and never implement. Native children cannot redelegate; Astra assigns bounded employees directly.

## Route

Inspect existing builders and concept briefs, implement native environments without changing camera presets, then serialize engine tests and captures. Freeze evidence, review independently, send concrete failures to the integrator for one refinement, re-score and report. Two attempts per failing gate. Continue unrelated work when a gate blocks.

## Launch evidence

Live checkout main; only pre-existing modification art/concepts/world-arenas-r1/README.md. No Godot or task-specific implementation process found in preflight. Native delegation configuration verified as opencode-go/deepseek-flash, explicitly accepted by Luca. No routing settings changed.

## Artifacts

This charter, MAP.md, LOG.md, tickets/, BOARD.md, captures and council ledger live in docs/mission/world-arenas/. Existing mission charter retains zero-spend bounds. Board reports state shipped, verified, blocked, budget, open decisions and next action. Cross-family review is constrained by the mandated single native model; this is an evidence gap, never a fabricated council pass.
