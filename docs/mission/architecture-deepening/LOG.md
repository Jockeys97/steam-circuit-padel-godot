# Architecture deepening operating log

## 2026-09-17

- Event: mission chartered.
- Owner: CEO.
- Status: ready.
- Decision: implement all five approved candidates in dependency order, with arena and audio as the only parallel mutation wave.
- Evidence: `CHARTER.md`, `MAP.md`, `tickets/*.md`.
- Budget: $0 paid APIs; 8 native worker launches; 2 retries per gate.

## 2026-09-17 — gate 5 (mixer contract), audio captain

- Event: gate 5 implementation returned.
- Owner: Audio captain.
- Status: done — `godot/src/audio/mixer_contract.gd` (new) is the one reader; `audio_port.gd` and `music.gd` both consume it; playback unchanged.
- Evidence (all under the one-engine directory lock): RED `FAIL 0/3` then GREEN `PASS 35/35` exit 0 on `res://tests/mixer_contract_test.gd`; `audio_port_test.gd` `PASS 15/15` exit 0; `music_port_test.gd` `PASS 32/32` exit 0; gdlint on the four changed files: 0 new findings.
- Changed paths: `godot/src/audio/mixer_contract.gd` (new), `godot/src/audio/audio_port.gd`, `godot/src/audio/music.gd`, `godot/tests/mixer_contract_test.gd` (new), `docs/mission/architecture-deepening/tickets/mixer-contract.md`, `docs/mission/architecture-deepening/LOG.md`. No commit; frozen paths untouched.
- Gap: each adapter still holds its own reader instance; one shared instance at match level is a later, non-blocking decision the reader supports.

## 2026-09-17 — gate 2 (arena catalog), arena captain

- Event: gate 2 implementation returned.
- Owner: Arena captain (one native worker launch; remaining launch budget 6).
- Status: implemented, ready for the wave-1 integration sweep.
- Decision: one catalog (`godot/game/arenas/arena_catalog.gd`) owns the frozen nine and the world five — display rows, lock facts, demo availability and the `{index, world, offered}` seat — bound to the build by `content_gate.gd::arena_catalog()` and read by both adapters (`UiData.arena_rows` / `world_arena_rows` for the recreated path, `Config.selectable_world_arenas` / `set_arena_id` / `apply_cli_selection` for the ported column). The world five are now reachable in the recreated arena screen under the legacy path's own rules; in a demo the set does not exist.
- Roster law kept: `Arena.ids()` is still the frozen nine, `Config.selectable_arenas()` is still 9 (full) / the demo's one, the career calendar rule is unchanged, and the world set never enters either list — asserted as section C of the new suite.
- TDD: `godot/tests/arena_catalog_test.gd` ran RED first (exit 1, `FAIL 7/12` full / `FAIL 7/8` demo, first failure the missing world cards) and then GREEN (exit 0, `PASS 15/15` full / `PASS 11/11` demo, 0 SCRIPT ERRORs); no assertion changed between the runs (the one test edit was naming the match-scene path, a gdlint line-length fix).
- Evidence: `tickets/arena-catalog.md` (RED and GREEN commands; regression tallies screen_arena_audit 133/133 + 120/120, demo_matrix_audit 133/133 + 178/178, data_audit 131/131 + 134/134, world_arenas_selection 18/18 + 8/8, field_law 88/88; gdlint: one new finding, `ArenaScreen.gd:1114 max-file-lines`, same class as the repo's other large screens).
- Changed paths: `godot/game/arenas/arena_catalog.gd` (new), `godot/game/content_gate.gd`, `godot/game/match_config.gd`, `godot/src/ui/data/UiData.gd`, `godot/src/ui/screens/ArenaScreen.gd`, `godot/tests/arena_catalog_test.gd` (new), `docs/mission/architecture-deepening/tickets/arena-catalog.md`, `docs/mission/architecture-deepening/LOG.md`. No commit; frozen paths untouched.
- Gap: the main harness and the UI integration suites are the integrator's gate-6 sweep (this lane's dispatch scopes it to the arena tests); `docs/mission/world-arenas/integrator.md` §9.1 records the recreated-lane gap this slice closes and should be reconciled by integration.

## 2026-09-17 — gate 1 (feedback vocabulary), feedback captain

- Event: gate 1 implementation returned.
- Owner: Feedback captain (one native worker launch; remaining launch budget 5).
- Status: done — `godot/game/feedback_vocabulary.gd` (new) is the one owner of the feedback vocabulary; the shipping match controller asks it directly, the legacy HUD only paints, `ViewState.gd` no longer keeps its own marker copy.
- Decision: the module owns the id encodings (`shot:<grade>` → `feedback_key`/`grade_of`, `shotMode:<mode>` → `mode_key`/`mode_of`), the words, the advice (`advice_of`/`advice_word`) and every colour the feedback is drawn with (grade/field-grade/energy/precision/advice + palette constants) plus the `UNREADABLE` marker and the resolver rule. Presentation stays where it was: the HUD's panels/log/chart, gate 3's court marks, the recreated path's view-model. No alias left behind: all nine `HudScript.<vocabulary>` call sites in `match_controller.gd` read the module, so the controller's only remaining use of the legacy HUD script is `HudScript.new()` — the mount, which gate 4 owns. `EVENT_LABELS`/`describe_event` stay in `hud.gd` because the generator and the coverage tool read that file (out of this ticket's scope).
- TDD: `godot/tests/feedback_vocabulary_test.gd` (new, 58 checks) ran RED against a knowledge-free stub (exit 1, `FAIL 5/58`, 54 failures, 0 SCRIPT ERRORs; first: the legacy HUD still declared the moved vocabulary and the controller had twelve `HudScript.` uses) and then GREEN (exit 0, `PASS 58/58`, 0 SCRIPT ERRORs). Expected values come from the existing rules/tests and the reference (`js/render.js`, `js/game.js`, `js/i18n.js`, the port's own suite literals), never from the new module.
- Evidence (all under the one-engine directory lock): `tests/timing_feedback_test.gd` exit 0 `PASS 100/100` with its `# tm` trace sha256 `401009a7…` byte-identical to the pre-change run (its private mode-key copy deleted, the trace now reads the module) — the machine-checked form of "behaviour moved, not changed"; `tests/game_slice_test.gd` exit 0 `PASS 342/342`, identical tally; `tests/ui/uir22_integration_audit.gd` exit 0 `PASS 71/71`; `tests/ui/router_audit.gd` exit 0 `PASS 86/86`. Anti-churn A/B: with the slice stashed, two `game_slice_test.gd` runs were 342/342 too, and the exit-time leaked-instance warning is a pre-existing nondeterministic audio-stream class (10/114 stashed vs 98–103 with the slice). gdlint: the new module and the new test clean; the four other changed scripts unchanged findings/counts; `hud.gd` 29 → 21 (the deleted block carried 8 `class-definitions-order` findings). `git diff --check` clean; frozen paths untouched; Wave-1 files keep their pre-slice mtimes; the pre-existing untracked UID/debris files untouched.
- Changed paths: `godot/game/feedback_vocabulary.gd` (new), `godot/tests/feedback_vocabulary_test.gd` (new), `godot/game/hud.gd`, `godot/game/match_controller.gd`, `godot/src/ui/ViewState.gd`, `godot/tests/game_slice_test.gd`, `godot/tests/timing_feedback_test.gd`, `docs/mission/architecture-deepening/tickets/feedback-vocabulary.md`, `docs/mission/architecture-deepening/LOG.md`. No commit; frozen paths untouched.
- Gap: a pre-existing divergence is recorded rather than fixed — for a successful smash the sim stores the mode as the grade already uppercased (`sim.gd:1795`) which the reference draws verbatim (`js/render.js:1068-1069`), while the port's resolver renders its marker; pinned "RECORDED, NOT ENDORSED" in the new test and left for a defect slice. The legacy HUD mount remains (gate 4). The integrator's gate-6 sweep (main harness, demo slice, audio suites, world-arena proof, JS audit) has not been run in this lane.

## 2026-09-17 — gate 1 (feedback vocabulary), feedback captain, retry 1 (acceptance correction)

- Event: the gate-1 acceptance gap is closed — the vocabulary module now owns the event log's knowledge.
- Owner: Feedback captain retry 1 (no native engine launch; remaining launch budget 4).
- Status: implemented; static checks green; scoped engine re-run blocked (a Godot process held the project for the whole retry) — handed to integration.
- Decision: `EVENT_LABELS`, `describe_event()` and `reason_label()` move from `godot/game/hud.gd` into `godot/game/feedback_vocabulary.gd` (one owner of grade, colour, label, advice and event knowledge). The HUD keeps only painting: `_update_log` asks `Vocabulary.describe_event(...)` — no alias, no second copy of the table. The generator (`godot/game/tools/gen_hud_labels.py`) now writes and `--check`-verifies the block in the module, and the coverage audit (`tools/i18n-port/hud-coverage.mjs`) reads the table from the module (left pointing at the HUD it would have read `{}` and counted all 108 ids as leaks).
- TDD: `godot/tests/feedback_vocabulary_test.gd` — `const EVENT_LABELS`, `static func describe_event`, `static func reason_label` move KEPT -> MOVED; new 12-check event-log section (a wiring check that goes red when the `Vocabulary.describe_event(` call is deleted from the HUD, the four resolver outcomes, the table's never-an-id and projection properties, the debt ledger). Expected tally 70 checks (58 + 12). `game_slice_test.gd`'s six `Hud.EVENT_LABELS`/`Hud.describe_event` references read the module.
- Evidence (static; engine busy): `python3 godot/game/tools/gen_hud_labels.py --check` exit 0 — "feedback_vocabulary.gd EVENT_LABELS is byte-identical to the generated block", 94 entries (84 projected + 10 ledger fallbacks); `python3 -m py_compile` exit 0; `node tools/i18n-port/hud-coverage.mjs --fail-on-leak` exit 0 — "108 ids — 108 get a readable line, 0 print the id (or contain it)"; the moved block is sha256 `7c1d60d8…` on both sides (committed `hud.gd` vs the module; resolver code bodies identical, only a docstring added); gdlint: module 0 -> 4 (the four >100-char generated lines moved with the block), `hud.gd` 21 -> 16, module test 0 -> 0, slice 121 -> 121; `git diff --check` clean.
- Changed paths: `godot/game/feedback_vocabulary.gd`, `godot/game/hud.gd`, `godot/game/tools/gen_hud_labels.py`, `tools/i18n-port/hud-coverage.mjs`, `godot/tests/feedback_vocabulary_test.gd`, `godot/tests/game_slice_test.gd`, `docs/mission/architecture-deepening/tickets/feedback-vocabulary.md`, `docs/mission/architecture-deepening/LOG.md`. No commit; frozen paths untouched by this captain.
- Blocker: engine verification (`feedback_vocabulary_test.gd`, `timing_feedback_test.gd`, `game_slice_test.gd`) could not run — PID 39787 held the project at the start and Godot editor PID 42103 (started 23:07:03) held it at the end; no engine launched and `/tmp/padel-godot-engine.lock` never taken. Integrator re-runs the three suites serially.
- Interference to repair: the Godot editor PID 42103 rewrote the frozen `godot/project.godot` at 23:07:06 (section reorder + comment loss) — not this captain; restore byte-for-byte from HEAD before the final commit.

## 2026-09-17 — gate 3 (court timing marks), court-feedback captain

- Event: gate 3 implementation returned.
- Owner: Court-feedback captain (no native engine launch; remaining launch budget 3).
- Status: implementation complete; static gates green; scoped engine RED/GREEN and the scoped suites BLOCKED — a user-owned Godot editor (PID 42103, at the start, at every checkpoint and at the end) held the project for the whole dispatch, so no engine was launched and `/tmp/padel-godot-engine.lock` was never taken. The runtime proof is handed to integration; the ticket says so.
- Decision: `godot/game/court_timing_marks.gd` (new, 701 lines) is the one owner of the court timing presentation — the 37 `TIMING_*` geometry constants with their reference anchors and frame measurements, the eleven marks, every builder, the per-frame placement, the verdict's rendering, the report and the capture's marker lines. Its seam is seven calls (`mount(parent) -> int`, `mark_names()`, `update(state, finished) -> Dictionary`, `report()`, `set_muted`, `is_muted`, `capture_lines(camera)`) — not a pass-through: it preloads `court.gd` and `feedback_vocabulary.gd` and nothing else, no caller (no cycle) and no sim script (it reads the state it is handed). `match_controller.gd` loses 531 lines and gains 30: one preload, one field, `CourtTiming.new()` + `.mount(self)` in `_build_scene`, ONE `.update(state, finished)` at the same point in `_sync_views` (before the athletes' early return), the A/B capture's mute + update, and the marker lines. The tree did not move, the owner did: same eleven nodes, same names, same build order, same parent. `timing_report()` forwards the module's report; a new `court_timing_marks()` accessor hands the seam to the tests.
- TDD: `godot/tests/court_timing_marks_test.gd` (new, 634 lines, 84 check call sites) is the seam's gate: the ownership scans, the marks' drawing contract, placement from a live state, the verdict, the A/B mute, the report and the capture lines. It ran RED first at the only place the engine cannot be faked — `/tmp/court-gate3/court_statscan.py`, which applies that suite's own A-section checks from its own constant lists — `FAIL 6/11` exit 1 with the shell still owning all 37 constants, all ten builders and the 22 private `_timing_*` names, then GREEN `PASS 11/11` exit 0 after the controller edit. Three surfaces are stated separately in the test (the shell's moved declarations, the module's own builders, the public seam), so a rename cannot hide a surviving alias.
- Evidence (static; engine busy): gdlint — the new module and the new test 0 findings; `match_controller.gd` 23 → 16 findings, same classes (21 → 14 max-line-length, two of them the moved `CAPTURE_*` strings split across adjacent literals with the printed line byte-identical — reconstructed against `git show HEAD:...`: 206/206 and 110/110 chars), `game_slice_test.gd` 121 → 118, same classes; `git diff --check` clean; frozen paths (`godot/src/sim/**`, `js/**`, `scripts/**`) 62 files byte-identical; neighbours (Wave-1 arena/audio, `feedback_vocabulary.gd`, `hud.gd`, `ViewState.gd`, `UiData.gd`, `ArenaScreen.gd`, the other suites) byte-identical; `godot/project.godot` byte-identical to this captain's pre-flight hash (the editor's earlier reorder untouched, not restored, not staged). Behaviour is a move: a normalised per-function diff against the pre-extraction controller is clean apart from the declared deltas (`mount`/`update` signatures, `self` → `_parent`, the verdict taking `state`, the report additions, `fill_extent` measured at the rebuild the fill already did).
- Changed paths: `godot/game/court_timing_marks.gd` (new), `godot/tests/court_timing_marks_test.gd` (new), `godot/game/match_controller.gd`, `godot/tests/game_slice_test.gd` (timing section), `docs/mission/architecture-deepening/tickets/court-timing-marks.md`, `docs/mission/architecture-deepening/LOG.md`. No commit; frozen paths untouched by this captain.
- Cross-seam gap for integration: `godot/tests/feedback_vocabulary_test.gd:141-142` asserts the match shell preloads the vocabulary module. Gate 3 moved the shell's last vocabulary consumer into the court timing module (which preloads the vocabulary itself), so that one check now reads false; its five neighbours still hold (emulated statically). Out of this captain's write set — the re-point (assert the court timing module instead of the shell) and gate 4's invalidated `HudScript.` neighbour need ONE reconciliation pass over that file, not one patch per gate. Recorded in the ticket with the exact one-line form.
- Gap: the runtime proof — `court_timing_marks_test.gd`, `game_slice_test.gd` (its `_timing_presentation` check call sites 36 → 40 against `HEAD`, so re-baseline the tally; every other section must keep its count) and `timing_feedback_test.gd` (`PASS 100/100`, `# tm` trace sha256 `401009a7…` expected byte-identical) — is owed to an engine owner, along with the `timing.png`/`timing-off.png` A/B re-capture (tracked evidence; a rewrite is a deliberate integration act).

## 2026-09-17 — gate 4 (UI mount policy), mount-policy captain

- Event: gate 4 implementation returned.
- Owner: Mount-policy captain (no native worker launch; remaining launch budget 2).
- Status: implementation complete; static gates green; the focused suite's RED/GREEN produced through its own source-contract predicates; runtime proof and the swept suites BLOCKED — a user-owned Godot editor (PID 42103, at the start, at every checkpoint and at the end) held the project for the whole dispatch, so no engine was launched and `/tmp/padel-godot-engine.lock` was never taken. Handed to integration; the ticket says so.
- Decision: the mount is a UI-mode question, never a clock question. `match_controller.gd::_ready()` computes `_ui_new = (not ui_legacy) and _arg(args, "--ui=", "new") != "legacy"`; the old combined `if _ui_new and engine_driven:` is gone, and `engine_driven` keeps only its own guard (`if not engine_driven:` in `_process`). The ported column is built behind `if not _ui_new:` — a recreated run no longer constructs it, hides it or refreshes it (the two `_run_capture` refreshes became null-guarded; every remaining `_hud.` use sits behind its own `_hud != null`). New public `var ui_legacy := false` (set before the tree; `--ui=legacy` is the same request from the command line) mirrors `main_menu.gd:87`. `game/mode_hud.gd` is the mode session's own panel, not the ported column — carried in both modes, unchanged. `godot/game/hud.gd` was NOT touched (no construction/refresh change needed).
- TDD: new `godot/tests/ui_mount_policy_test.gd` (355 lines, gdlint clean) — section 1 is the source contract with its needles as named constants; sections 2-3 drive the REAL Match scene through the production path (`harness_mode()` before the tree, `tick_fixed()`, `apply_frame()`, `_unhandled_input` ESC → `HudLayer/PauseOverlay`, `report()` only; no private `_ui_hud`/`_paused`/`_bridge`, no HUD mounted by the test). RED first: `/tmp/mount-gate4/mount_statscan.py` applies the suite's own section-1 predicates — `FAIL 2/5` exit 1 against `git show HEAD:godot/game/match_controller.gd` (that revision carries the same mount condition the working copy had), the three failures being the combined condition, the unconditional `HudScript.new()`, and three unguarded ported-column uses; then GREEN `PASS 5/5` exit 0 after the change. The scanner states plainly that the runtime sections are not emulated.
- Coverage migration (the shipping seam): slice `_hud_reflects_state()` reads the mounted `HudLayer/UiHud`'s public `report()` (player/ai scores, rendered log, no-raw-id scan over every rendered string), asserts `HudLayer/Hud` is ABSENT in a shipping run, and asserts the finished match's result through `result_payload()`; `_visual_contract` / `_hud_safe_area` — whose subject IS the ported column — now ask for it explicitly via `_new_match_node(0, true)` (the documented switch, not a CLI flag); `uir22_integration_audit.gd`'s harness match asserts `HudLayer/UiHud` + `HudLayer/PauseOverlay` mounted with the clock off and no ported column; the court captain's recorded cross-seam one-liner landed in `feedback_vocabulary_test.gd::_ownership()` (the source-location check re-points to `godot/game/court_timing_marks.gd`, the `HudScript.` neighbour renamed to the legacy mount, its use count still 1).
- Evidence (static; engine busy): gdlint — new suite "Success: no problems found"; per-file finding multisets compared against pre-edit files reconstructed by reversing this captain's own hunks (the controller reconstruction is byte-identical to the pre-edit file: sha256[:16] `651f2cb925b343fe`, taken before the first edit): `match_controller.gd` 16 → 16, same classes; `game_slice_test.gd` 118 → 115 with zero added long lines (the three removed were the legacy-label assertions that left with the migration); `uir22_integration_audit.gd` 30 → 30; `feedback_vocabulary_test.gd` unchanged; `git diff --check` clean; frozen paths untouched; `godot/project.godot` still the editor's 23:07 rewrite (not restored, not staged, not touched by this captain).
- Changed paths: `godot/game/match_controller.gd`, `godot/tests/ui_mount_policy_test.gd` (new), `godot/tests/game_slice_test.gd`, `godot/tests/ui/uir22_integration_audit.gd`, `godot/tests/feedback_vocabulary_test.gd`, `docs/mission/architecture-deepening/tickets/ui-mount-policy.md`, `docs/mission/architecture-deepening/LOG.md`. No commit.
- Gaps: (1) the runtime sweep is owed to an engine owner — the focused suite RED-before/GREEN-after in-engine, `game_slice_test.gd` full and `-- --demo`, `uir22_integration_audit.gd`, `replay_audit.gd`, `timing_feedback_test.gd` (its `# tm` trace sha256 `401009a7…` must stay byte-identical: no sim input was touched). (2) The slice's `objects_delta <= 450` ceiling is the one number this gate could move — every harness node now mounts four UI scenes freed with their node, against a 395 baseline measured when harness runs mounted nothing. (3) Two legacy-only assertions were replaced rather than ported (the ported debug line's seed/tier, the legacy result panel's visibility): the shipping counterparts are asserted through `result_payload()`, the ported panels still by `_visual_contract`/`_hud_safe_area` and the new suite's legacy section. (4) Recorded, not fixed: the recreated log does not drop a line identical to the one above it while `hud.gd::_update_log` does — a pre-existing view-model divergence, outside this gate.

## 2026-09-18 — independent review returned; reconciliation and repairs (final integration captain)

- Event: the independent review (`/tmp/architecture-deepening-independent-review.md`) returned **spec FAIL / standards FAIL** with a correction contract F-01…F-12; the final integration captain reconciled every finding, ran the integrated proof and made the one local commit.
- Owner: Final Integration captain (this launch; no delegation, no child processes left running; remaining launch budget 0).
- Status: **done — all five gates shipped, contract discharged, proof green, commit made (local only, not pushed).**
- Decision — repairs (each re-verified in the engine):
  - F-01 `game_slice_test.gd:1585/1589` `String(...)` → `str(...)`: the three previously skipped recreated-HUD checks (no-raw-id scan, result payload, win flag) now run and pass.
  - F-02 `_drop(node)` + `_section_done("_timing_presentation")` at the section's normal end: `# sections ran 23/23` in both lineages.
  - F-03 `court_timing_marks_test._placement()` calls `update()` before its first report read, and the suite gained a slice-style section guard (`SECTIONS`/`_section_done`, checked at the end of `_run`) so a thrown section can never tally green; the C section ran in the engine for the first time, green.
  - F-04 re-baselined with measurement, not a blind bump: full `518/520/518`, demo `537/537`, `nodes=1 orphans=0` every run; ceiling 450 → **600** with the numbers and the bounded margin in the check's own comment.
  - F-05 `await process_frame` after the mount, before the capture section: the 12 `ERROR: Condition "!is_inside_tree()"` lines → **0**; no allowance added.
  - F-06…F-09 restored **byte-for-byte** from `e4a1aff` (`project.godot`, `SmokeTest.tscn`, `padel_theme.tres`, `maestro-rigged_texture_0.png.import`); all four gone from `git status`.
  - F-10 executed: `arena_catalog.demo()`/`offered()`, `mixer_contract.bus_plan()`, `court_timing_marks.is_muted()` deleted; the two suites rewritten to the remaining public surface (the mixer check now drives `ensure_bus` + the real AudioServer; the mute is asserted through the frame it produces); no dangling references in `godot/**`.
  - F-11 accepted: the thin forwarding wrappers (`audio_port._mixer()`, the adapters' `reference_*`/`muted_default`/`master_gain_range` forwards, `match_config.arena_catalog()`) stay as compatibility seams — recorded, no change.
  - F-12 resolved: slice tally re-baselined 342 → **345** full / 293 → **296** demo; a per-check sequence comparison against the review's own logs shows every other check identical, same order.
- Evidence — integrated proof (serial, `/tmp/padel-godot-engine.lock`, `pgrep` preflight + trap cleanup; logs `/tmp/archd-final/`, manifest `manifest-final.txt`): main harness `PASS 8/8`; feedback vocabulary `70/70`; court timing `87/87` (0 SCRIPT ERROR, 0 ERROR); ui mount policy `22/22`; arena catalog `15/15` + demo `11/11`; mixer contract `35/35`; timing feedback `100/100` (`timingSha256=401009a7cc07a7d6…`, absent drift = red); slice `345/345` and demo `296/296`; uir22 `75/75`; replay audit `166/166`; audio `15/15`; music `32/32`; world arenas selection `18/18`; field law `88/88`; input `run_all` `5/5`; audits `run_all` `10/10`. Every `ERROR:` line is a named class (slice arena guard ×1, shutdown resource line ≤1, mixer reader's two ok-flanked refusal probes). Static: `gen_hud_labels.py --check` exit 0 (94 entries); `hud-coverage.mjs --fail-on-leak` exit 0 (108/108, 0 leaks); `git diff --check` clean; frozen paths (`js/**`, `scripts/**`, `godot/src/sim/**`) **0 changed files**; gdlint: no new class on any changed file (equal or fewer; `ArenaScreen`'s documented `max-file-lines` +1 / `max-line-length` −1 exchange; only the 4 documented `max-line-length` on `feedback_vocabulary.gd`'s generated block).
- Inherited red (recorded, not fixed): `npm run audit` exit 1, **25/28** — identical to the pre-mission baseline with byte-identical inputs (`assets-audit` = js-side unreferenced `assets/arenas/officina-vapore.webp`; `outfit-assets-audit` assertion; `unlockable-animation-audit` = `Cannot find package 'sharp'`, environment). The frozen reference was not edited to make these pass.
- Records: five tickets → done with verified tallies; `MAP.md` frontier → complete; `board-2026-09-18.md` written (shipped / gate status / $0 / inherited npm gap / commit-ready scope); root `CONTEXT.md` written as a glossary of the resolved terms (reference, port, frozen roster, world arena, match feedback, court timing marks, recreated UI, legacy UI, engine-driven match, mixer contract, parity, frozen harness, owner gate) — definitions taken from existing docs, no ADR.
- Changed paths (staged): the four new modules, five new suites + their `.gd.uid` sidecars, the twelve modified mission files listed in the board, `tools/i18n-port/hud-coverage.mjs`, `CONTEXT.md`, `docs/mission/architecture-deepening/**`, this log. Restored (unstaged): the four F-06…F-09 files. Pre-existing untracked debris (`godot/=/**`, old probe/audit `.gd.uid`s) left byte-for-byte untouched and unstaged.
- Gaps (honest): the `timing.png`/`timing-off.png` A/B frames and the `shots*` captures were **not** re-run (outside the proof contract; the engine-driven recreated path is unchanged except two removed no-op legacy refreshes); `world_arenas_selection_test` printed the intermittent shutdown resource line this run (allowed by name; absent in the review's run); the smash mode's stored-uppercase divergence stays recorded, not endorsed.

## 2026-09-18 — push (owner instruction), CEO

- Event: the owner instructed commit-and-push; the mission's single commit is now on `origin/main`.
- Owner: CEO (parent session, no worker launch).
- Status: **pushed.**
- Decision: none — mechanical publication of the already-verified commit. No file content changed in this step.
- Evidence: pre-push `git ls-remote origin -h refs/heads/main` = `e4a1aff2863c87086cccfe5bdd0bf5b19571fc12` (exactly the commit's parent, so a fast-forward, no rebase and no conflict resolution); `git push origin main` → `e4a1aff..3228c94  main -> main`; read-back `git ls-remote` = `3228c947f3e9821f20c1826be51dcf8d389c5763` = local `git rev-parse HEAD`; `git status --short --branch` shows `## main...origin/main` (in sync) with only the 16 pre-existing untracked debris paths, unstaged. Repo-local `commit.gpgsign=false` unchanged; the global git config was not touched.
- Gap (honest): the proof in this log was run on the pre-push tree; the push itself re-ran no suite (it changes no tracked content). The board's "local only" wording was corrected in the follow-up doc commit.
