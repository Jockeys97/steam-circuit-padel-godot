# Architecture deepening map

## Destination

The port keeps identical player behavior while five high-churn concerns gain clear ownership and testable seams.

## Route

1. Wave 1: arena catalog and mixer contract in parallel. Their write sets do not overlap.
2. Wave 2: feedback vocabulary. This must land before UI mount policy.
3. Wave 3: court timing marks. This owns the first large `match_controller.gd` extraction.
4. Wave 4: mount policy. It works on the integrated controller after court timing extraction.
5. Integration: glossary, cross-seam reconciliation, serial proof sweep.
6. Independent review, repair if needed, final proof, one commit.

## Fixed decisions

- Frozen reference and sim paths remain untouched.
- The recreated UI is the shipping UI path. Legacy remains only where the frozen harness still needs it.
- Tests should cross the same seams as production callers.
- The final integration captain is the only owner of cross-team reconciliation and the commit.

## Fog

- Whether legacy HUD construction can be fully removed depends on harness use found during gate 1.
- The exact court-feedback module shape depends on preserving capture and report behavior.
- A CONTEXT.md glossary does not exist yet. Integration creates it only for resolved domain terms, with no implementation detail.

## Current frontier

Complete — all five gates implemented, the independent review's correction contract
(F-01…F-12) discharged, the integrated proof green (all suites, harness, slices,
input/audits runners, static gates), `CONTEXT.md` written, and the single local commit
made 2026-09-18. See `board-2026-09-18.md` and `LOG.md`.
