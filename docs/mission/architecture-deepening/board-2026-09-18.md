# Board — architecture deepening, final integration (2026-09-18)

Outcome: the five approved deepening candidates are shipped and the independent
review's correction contract (F-01…F-12) is discharged. One local commit; nothing
pushed. Spend $0 (no paid APIs; native launch budget now 0).

## What shipped

One owner each, behaviour unchanged, proven by the suites that cross each seam:

- **Match feedback vocabulary** (`godot/game/feedback_vocabulary.gd`) — ids, words,
  colours, advice, `UNREADABLE` and the event log's table; the legacy HUD only paints.
- **Arena catalog** (`godot/game/arenas/arena_catalog.gd`) — the frozen nine and the
  world five, one seat rule for both UI paths; world arenas reachable in the recreated
  screen.
- **Court timing marks** (`godot/game/court_timing_marks.gd`) — geometry, placement,
  verdict, report, capture lines out of `match_controller.gd`.
- **UI mount policy** (`match_controller.gd`) — the mount is a UI-mode question; the
  recreated UI is the shipping path and a real-match test crosses it.
- **Mixer contract** (`godot/src/audio/mixer_contract.gd`) — one reader both audio
  adapters consume; playback stays in the adapters.
- **`CONTEXT.md`** (repo root) — the glossary of the resolved domain terms these
  modules speak; no implementation detail.

## Gate status — the integrated proof (all serial, under `/tmp/padel-godot-engine.lock`)

| Run | Exit | Tally | SCRIPT ERROR | ERROR lines |
|---|---|---|---|---|
| main harness (SmokeTest main scene) | 0 | PASS 8/8 | 0 | 0 |
| feedback_vocabulary_test | 0 | PASS 70/70 | 0 | 0 |
| court_timing_marks_test | 0 | **PASS 87/87** | 0 | **0 (was 12)** |
| ui_mount_policy_test | 0 | PASS 22/22 | 0 | 1 shutdown |
| arena_catalog_test / `-- --demo` | 0 / 0 | PASS 15/15 / 11/11 | 0 | 0 |
| mixer_contract_test | 0 | PASS 35/35 | 0 | 2 refusal probes |
| timing_feedback_test | 0 | PASS 100/100 | 0 | 0 — `timingSha256=401009a7…` |
| game_slice_test | 0 | **PASS 345/345** | 0 | arena guard + shutdown |
| game_slice_test `-- --demo` | 0 | **PASS 296/296** | 0 | arena guard + shutdown |
| uir22_integration_audit | 0 | PASS 75/75 | 0 | 0 |
| replay_audit | 0 | PASS 166/166 | 0 | 1 shutdown |
| audio_port_test | 0 | PASS 15/15 | 0 | 1 shutdown |
| music_port_test | 0 | PASS 32/32 | 0 | 0 |
| world_arenas_selection_test | 0 | PASS 18/18 | 0 | 1 shutdown |
| world_arenas_field_law_test | 0 | PASS 88/88 | 0 | 0 |
| input/run_all | 0 | PASS 5/5 | 0 | 0 |
| audits/run_all | 0 | PASS 10/10 | 0 | 0 |

Every `ERROR:` line above is a named class: the slice's deliberately provoked arena
guard (×1, allowed by `godot/game/check_log.sh`), the engine's shutdown resource line
(≤1, the same allowance; intermittent, and present/absent across suites), and the
mixer reader's two deliberate refusal probes (impostor schema, missing file — asserted
by the checks beside them; the repo's own convention names this the "ok-flanked
refusal" class). No SCRIPT ERROR anywhere; no allowance was added to `check_log.sh`.

Static gates: `gen_hud_labels.py --check` exit 0 (94 entries, byte-identical);
`hud-coverage.mjs --fail-on-leak` exit 0 (108 ids — 108 readable, 0 leaks);
`git diff --check` clean; frozen paths (`js/**`, `scripts/**`, `godot/src/sim/**`)
0 changed files; gdlint shows no new class on any changed file (equal or fewer,
ArenaScreen's documented `max-file-lines` +1 / `max-line-length` −1 exchange the only
move) and only the documented 4 `max-line-length` on the generated block of
`feedback_vocabulary.gd`.

## Review corrections (F-01…F-12)

- **F-01** fixed — `str()` in the recreated-HUD scan; the 3 skipped checks run.
- **F-02** fixed — `_drop(node)` + `_section_done("_timing_presentation")`; sections 23/23.
- **F-03** fixed — `update()` before the first report read; a slice-style section guard
  added; the court suite's C section runs in the engine for the first time, green.
- **F-04** fixed — measured ceiling: full 518/520/518, demo 537/537, `nodes=1
  orphans=0` every run; ceiling raised 450 → 600 with the numbers and the bounded
  margin recorded in the check's own comment.
- **F-05** fixed — `await process_frame` after the mount; 12 engine ERRORs → 0.
- **F-06…F-09** restored byte-for-byte from `e4a1aff`; all four are gone from git status.
- **F-10** executed — `arena_catalog.demo()`/`offered()`, `mixer_contract.bus_plan()`,
  `court_timing_marks.is_muted()` deleted; tests rewritten to the remaining public
  surface; no dangling references.
- **F-11** accepted — the thin forwarding wrappers stay as compatibility seams (recorded).
- **F-12** resolved — the slice tally is re-baselined: 342 → **345** (full; the three
  recovered checks) and 293 → **296** (demo); a check-sequence comparison against the
  review's own logs shows every other check identical, same order.

## Inherited / environmental reds (recorded, not fixed)

- `npm run audit`: **exit 1, 25/28** — identical to the review's baseline with
  byte-identical inputs. `assets-audit` (js-side unreferenced
  `assets/arenas/officina-vapore.webp`), `outfit-assets-audit` (assertion) and
  `unlockable-animation-audit` (`Cannot find package 'sharp'` — environment). The
  frozen reference is not edited to make these pass; they are not mission-attributable.
- `world_arenas_selection_test` printed the engine's shutdown resource line this run
  (the review's run did not; the class is intermittent and allowed by name).

## Commit scope (mission-attributable only)

New: `godot/game/feedback_vocabulary.gd`, `godot/game/court_timing_marks.gd`,
`godot/game/arenas/arena_catalog.gd`, `godot/src/audio/mixer_contract.gd`, the five new
suites (`feedback_vocabulary_test`, `court_timing_marks_test`, `arena_catalog_test`,
`mixer_contract_test`, `ui_mount_policy_test`), their `.gd.uid` sidecars,
`CONTEXT.md`, `docs/mission/architecture-deepening/**`.
Modified: `content_gate.gd`, `hud.gd`, `match_config.gd`, `match_controller.gd`,
`gen_hud_labels.py`, `audio_port.gd`, `music.gd`, `ViewState.gd`, `UiData.gd`,
`ArenaScreen.gd`, `game_slice_test.gd`, `timing_feedback_test.gd`,
`uir22_integration_audit.gd`, `tools/i18n-port/hud-coverage.mjs`.
Restored (not staged): `godot/project.godot`, `godot/tests/SmokeTest.tscn`,
`godot/src/ui/theme/padel_theme.tres`,
`godot/assets/athletes/maestro-rigged_texture_0.png.import`.
Pre-existing untracked debris (`godot/=/**`, old probe/audit `.gd.uid` files) remains
byte-for-byte untouched and unstaged.

Commit: `refactor: deepen match architecture seams` by Luca Fantini
<lucadefantini@gmail.com>, local only. $0 spend. Remaining launch budget: 0.
