extends Resource
## Offline-rendered samples for the original procedural match theme. The sequencer
## still chooses notes and gains at runtime; only waveform synthesis is cached.

@export var voices: Dictionary = {}
