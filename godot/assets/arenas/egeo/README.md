# egeo kit

Drop `<slot>.glb` here (`hero_landmark`, `gate_portal`, `light_source`, `vegetation_cluster`,
`ground_dressing`, `ornament_accent`, `column_pillar`, `railing_segment`, `furniture`,
`signage_banner`) and the engine mounts it. Anchors, target heights and repeat counts for this
arena are in `godot/game/arenas/arena_kit.gd` (`SPECS["egeo"]`); the recipe is
`docs/mission/arena-kit/PIPELINE.md`. No file here = the procedural blockout stands.
